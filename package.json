{
  "name": "api-gateway",
  "bin": {
    "api": "./dist/cli.js"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "predev": "npm run build -w shared",
    "predev:lan": "npm run build -w shared",
    "dev": "concurrently --kill-others-on-fail --names server,client \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:lan": "concurrently --kill-others-on-fail --names server,client \"npm run dev -w server\" \"npm run dev -w client -- --host\"",
    "pretest": "npm run build -w shared",
    "test": "npm run test -w server && npm run typecheck -w client && npm run lint -w client",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client && npm run build:cli",
    "build:server": "npm run build -w shared && npm run build -w server",
    "build:cli": "node_modules/.bin/esbuild scripts/cli.ts --bundle --platform=node --format=esm --target=node22 --banner:js='#!/usr/bin/env node' --outfile=dist/cli.js"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "concurrently": "^9.1.2"
  }
}
